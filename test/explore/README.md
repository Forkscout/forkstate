# Exploration scripts

These are not tests. They print rather than assert, they need a server already
running, and they exist to answer a question once — how deep a provider serves
archive reads, how many environments a process holds, what a transfer costs.

The suite in `../suite.ts` is what has to keep passing.
